import { Link } from 'react-router-dom'
import { useState } from 'react'

export default function Login(){const [register,setRegister]=useState(false);return <section className="section auth-page"><div className="auth-card"><span className="eyebrow">SOFT-GATE ACCOUNT</span><h1>{register?'Create your account':'Welcome back.'}</h1><p>{register?'Create an account to track orders and manage your details.':'Sign in to manage your orders and account.'}</p><form onSubmit={e=>e.preventDefault()}>{register&&<label>Full name<input required placeholder="Full name"/></label>}<label>Email address<input type="email" required placeholder="you@example.com"/></label><label>Password<input type="password" required placeholder="••••••••"/></label><button className="primary-button full">{register?'Create account':'Sign in'}</button></form><button className="switch-auth" onClick={()=>setRegister(!register)}>{register?'Already have an account? Sign in':'New here? Create an account'}</button><Link to="/shop">Continue shopping →</Link></div></section>}
