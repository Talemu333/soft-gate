import { Link } from 'react-router-dom'
import { products, formatPrice } from '../data/products'

export default function Cart(){const items=[{p:products[0],q:1},{p:products[5],q:1}];const subtotal=items.reduce((s,i)=>s+i.p.price*i.q,0);return <section className="section cart-page"><div className="page-intro"><span className="eyebrow">YOUR CART</span><h1>Ready to check out?</h1></div><div className="cart-layout"><div className="cart-items">{items.map(i=><div className="cart-item" key={i.p.id}><div className="cart-thumb">▱</div><div><h3>{i.p.name}</h3><p>{formatPrice(i.p.price)} · Qty {i.q}</p></div><strong>{formatPrice(i.p.price*i.q)}</strong></div>)}</div><aside className="summary"><span>ORDER SUMMARY</span><div><span>Subtotal</span><b>{formatPrice(subtotal)}</b></div><div><span>Delivery</span><b>Calculated at checkout</b></div><hr/><div><strong>Total</strong><strong>{formatPrice(subtotal)}</strong></div><Link className="primary-button full" to="/checkout">Proceed to checkout →</Link></aside></div></section>}
